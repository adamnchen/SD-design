<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sutran.sd.design.mapper.SdPresaleProjectMapper">

    <resultMap type="com.sutran.sd.design.domain.SdPresaleProject" id="SdPresaleProjectResult">
        <result property="id" column="id"/>
        <result property="projectNo" column="project_no"/>
        <result property="title" column="title"/>
        <result property="description" column="description"/>
        <result property="coverImage" column="cover_image"/>
        <result property="images" column="images"/>
        <result property="videoUrl" column="video_url"/>
        <result property="tags" column="tags"/>
        <result property="creatorUserId" column="creator_user_id"/>
        <result property="creatorName" column="creator_name"/>
        <result property="creatorAvatar" column="creator_avatar"/>
        <result property="manufacturerUserId" column="manufacturer_user_id"/>
        <result property="manufacturerName" column="manufacturer_name"/>
        <result property="manufacturerAvatar" column="manufacturer_avatar"/>
        <result property="proofingInvitationId" column="proofing_invitation_id"/>
        <result property="basePrice" column="base_price"/>
        <result property="tieredPricing" column="tiered_pricing"/>
        <result property="validityDays" column="validity_days"/>
        <result property="status" column="status"/>
        <result property="productionStatus" column="production_status"/>
        <result property="deliveryStatus" column="delivery_status"/>
        <result property="viewCount" column="view_count"/>
        <result property="favoriteCount" column="favorite_count"/>
        <result property="shareCount" column="share_count"/>
        <result property="manufacturerPhotos" column="manufacturer_photos"/>
        <result property="manufacturerUploadTime" column="manufacturer_upload_time"/>
        <result property="totalSalesAmount" column="total_sales_amount"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <sql id="selectSdPresaleProjectVo">
        select id, project_no, title, description, cover_image, images, video_url, tags,
               creator_user_id, creator_name, creator_avatar, manufacturer_user_id, manufacturer_name, manufacturer_avatar,
               proofing_invitation_id, base_price, tiered_pricing, validity_days,
               status, production_status, delivery_status, view_count, favorite_count, share_count,
               manufacturer_photos, manufacturer_upload_time, total_sales_amount,
               create_by, create_time, update_by, update_time
        from sd_presale_project
    </sql>

    <update
        id="dealExpireData">
        update sd_presale_project
        set status = 3
        where status = 1 AND DATE_ADD(create_time, INTERVAL validity_days DAY) &lt;= #{now}
    </update>

    <select id="selectSdPresaleProjectById" parameterType="Long" resultMap="SdPresaleProjectResult">
        <include refid="selectSdPresaleProjectVo"/>
        where id = #{id}
    </select>

    <select id="selectSdPresaleProjectList" parameterType="com.sutran.sd.design.domain.SdPresaleProject" resultMap="SdPresaleProjectResult">
        <include refid="selectSdPresaleProjectVo"/>
        <where>
            <if test="projectNo != null and projectNo != ''">
                AND project_no = #{projectNo}
            </if>
            <if test="title != null and title != ''">
                AND title like concat('%', #{title}, '%')
            </if>
            <if test="creatorUserId != null">
                AND creator_user_id = #{creatorUserId}
            </if>
            <if test="manufacturerUserId != null">
                AND manufacturer_user_id = #{manufacturerUserId}
            </if>
            <if test="proofingInvitationId != null">
                AND proofing_invitation_id = #{proofingInvitationId}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        order by create_time desc
    </select>

    <select id="selectPagePresaleProjectList" resultMap="SdPresaleProjectResult">
        <include refid="selectSdPresaleProjectVo"/>
        <where>
            <if test="sdPresaleProject.projectNo != null and sdPresaleProject.projectNo != ''">
                AND project_no = #{sdPresaleProject.projectNo}
            </if>
            <if test="sdPresaleProject.title != null and sdPresaleProject.title != ''">
                AND title like concat('%', #{sdPresaleProject.title}, '%')
            </if>
            <if test="sdPresaleProject.creatorUserId != null">
                AND creator_user_id = #{sdPresaleProject.creatorUserId}
            </if>
            <if test="sdPresaleProject.manufacturerUserId != null">
                AND manufacturer_user_id = #{sdPresaleProject.manufacturerUserId}
            </if>
            <if test="sdPresaleProject.proofingInvitationId != null">
                AND proofing_invitation_id = #{sdPresaleProject.proofingInvitationId}
            </if>
            <if test="sdPresaleProject.status != null">
                AND status = #{sdPresaleProject.status}
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

    <select id="selectByCrowdfundingProjectId" parameterType="Long" resultMap="SdPresaleProjectResult">
        <include refid="selectSdPresaleProjectVo"/>
        where proofing_invitation_id = #{crowdfundingProjectId}
    </select>

    <!-- 多表联查预售项目列表（带用户信息） -->
    <select id="selectPresaleProjectListWithUserInfo" resultMap="SdPresaleProjectResult">
        SELECT
            p.id, p.project_no, p.title, p.description, p.cover_image, p.images, p.video_url, p.tags,
            p.creator_user_id, p.creator_name, p.creator_avatar,
            p.manufacturer_user_id, p.manufacturer_name, p.manufacturer_avatar,
            p.proofing_invitation_id, p.base_price, p.tiered_pricing, p.validity_days,
            p.status, p.production_status, p.delivery_status, p.view_count, p.favorite_count, p.share_count,
            p.manufacturer_photos, p.manufacturer_upload_time, p.total_sales_amount,
            p.create_by, p.create_time, p.update_by, p.update_time,
            -- 关联查询发起人信息
            creator.nick_name as creator_nick_name,
            creator.avatar as creator_avatar_url,
            -- 关联查询厂家信息
            manufacturer.nick_name as manufacturer_nick_name,
            manufacturer.avatar as manufacturer_avatar_url
        FROM sd_presale_project p
        LEFT JOIN sys_user creator ON p.creator_user_id = creator.user_id
        LEFT JOIN sys_user manufacturer ON p.manufacturer_user_id = manufacturer.user_id
        <where>
            <if test="status != null">
                AND p.status = #{status}
            </if>
            <!-- 如果是销售中状态，需要检查是否过期 -->
            <if test="status != null and status == 1">
                AND DATE_ADD(p.create_time, INTERVAL p.validity_days DAY) &gt; #{now}
            </if>
        </where>
        ORDER BY p.create_time DESC
    </select>

    <!-- 多表联查预售项目列表（带用户信息） -->
    <select id="selectPresaleProjectListWithUserInfoOfPage" resultMap="SdPresaleProjectResult">
        SELECT
        p.id, p.project_no, p.title, p.description, p.cover_image, p.images, p.video_url, p.tags,
        p.creator_user_id, p.creator_name, p.creator_avatar,
        p.manufacturer_user_id, p.manufacturer_name, p.manufacturer_avatar,
        p.proofing_invitation_id, p.base_price, p.tiered_pricing, p.validity_days,
        p.status, p.production_status, p.delivery_status, p.view_count, p.favorite_count, p.share_count,
        p.manufacturer_photos, p.manufacturer_upload_time, p.total_sales_amount,
        p.create_by, p.create_time, p.update_by, p.update_time,
        -- 关联查询发起人信息
        creator.nick_name as creator_nick_name,
        creator.avatar as creator_avatar_url,
        -- 关联查询厂家信息
        manufacturer.nick_name as manufacturer_nick_name,
        manufacturer.avatar as manufacturer_avatar_url
        FROM sd_presale_project p
        LEFT JOIN sys_user creator ON p.creator_user_id = creator.user_id
        LEFT JOIN sys_user manufacturer ON p.manufacturer_user_id = manufacturer.user_id
        <where>
            <if test="status != null">
                AND p.status = #{status}
            </if>
            <!-- 如果是销售中状态，需要检查是否过期 -->
            <if test="status != null and status == 1">
                AND DATE_ADD(p.create_time, INTERVAL p.validity_days DAY) &gt; #{now}
            </if>
        </where>
        ORDER BY p.create_time DESC
    </select>

    <!-- 多表联查用户参与的预售项目列表 -->
    <select id="selectUserPresaleProjects" resultMap="SdPresaleProjectResult">
        SELECT
            p.id, p.project_no, p.title, p.description, p.cover_image, p.images, p.video_url, p.tags,
            p.creator_user_id, p.creator_name, p.creator_avatar,
            p.manufacturer_user_id, p.manufacturer_name, p.manufacturer_avatar,
            p.proofing_invitation_id, p.base_price, p.tiered_pricing, p.validity_days,
            p.status, p.production_status, p.delivery_status, p.view_count, p.favorite_count, p.share_count,
            p.manufacturer_photos, p.manufacturer_upload_time, p.total_sales_amount,
            p.create_by, p.create_time, p.update_by, p.update_time,
            -- 关联查询发起人信息
            creator.nick_name as creator_nick_name,
            creator.avatar as creator_avatar_url,
            -- 关联查询厂家信息
            manufacturer.nick_name as manufacturer_nick_name,
            manufacturer.avatar as manufacturer_avatar_url
        FROM sd_presale_project p
        LEFT JOIN sys_user creator ON p.creator_user_id = creator.user_id
        LEFT JOIN sys_user manufacturer ON p.manufacturer_user_id = manufacturer.user_id
        <where>
            <choose>
                <when test="userType == 'creator'">
                    AND p.creator_user_id = #{userId}
                </when>
                <when test="userType == 'manufacturer'">
                    AND p.manufacturer_user_id = #{userId}
                </when>
                <when test="userType == 'buyer'">
                    AND p.id IN (
                        SELECT DISTINCT project_id
                        FROM sd_presale_order
                        WHERE user_id = #{userId}
                        AND order_status IN (2, 3, 4, 5)
                    )
                </when>
            </choose>
        </where>
        ORDER BY p.create_time DESC
    </select>

    <!-- 插入预售项目 -->
    <insert id="insertSdPresaleProject" parameterType="com.sutran.sd.design.domain.SdPresaleProject" useGeneratedKeys="true" keyProperty="id">
        insert into sd_presale_project
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="projectNo != null and projectNo != ''">project_no,</if>
            <if test="title != null and title != ''">title,</if>
            <if test="description != null">description,</if>
            <if test="coverImage != null">cover_image,</if>
            <if test="images != null">images,</if>
            <if test="videoUrl != null">video_url,</if>
            <if test="tags != null">tags,</if>
            <if test="creatorUserId != null">creator_user_id,</if>
            <if test="creatorName != null and creatorName != ''">creator_name,</if>
            <if test="creatorAvatar != null">creator_avatar,</if>
            <if test="manufacturerUserId != null">manufacturer_user_id,</if>
            <if test="manufacturerName != null and manufacturerName != ''">manufacturer_name,</if>
            <if test="manufacturerAvatar != null">manufacturer_avatar,</if>
            <if test="proofingInvitationId != null">proofing_invitation_id,</if>
            <if test="basePrice != null">base_price,</if>
            <if test="tieredPricing != null">tiered_pricing,</if>
            <if test="validityDays != null">validity_days,</if>
            <if test="status != null">status,</if>
            <if test="productionStatus != null">production_status,</if>
            <if test="deliveryStatus != null">delivery_status,</if>
            <if test="viewCount != null">view_count,</if>
            <if test="favoriteCount != null">favorite_count,</if>
            <if test="shareCount != null">share_count,</if>
            <if test="manufacturerPhotos != null">manufacturer_photos,</if>
            <if test="manufacturerUploadTime != null">manufacturer_upload_time,</if>
            <if test="totalSalesAmount != null">total_sales_amount,</if>
            <if test="createBy != null">create_by,</if>
            <if test="createTime != null">create_time,</if>
            <if test="updateBy != null">update_by,</if>
            <if test="updateTime != null">update_time,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="projectNo != null and projectNo != ''">#{projectNo},</if>
            <if test="title != null and title != ''">#{title},</if>
            <if test="description != null">#{description},</if>
            <if test="coverImage != null">#{coverImage},</if>
            <if test="images != null">#{images},</if>
            <if test="videoUrl != null">#{videoUrl},</if>
            <if test="tags != null">#{tags},</if>
            <if test="creatorUserId != null">#{creatorUserId},</if>
            <if test="creatorName != null and creatorName != ''">#{creatorName},</if>
            <if test="creatorAvatar != null">#{creatorAvatar},</if>
            <if test="manufacturerUserId != null">#{manufacturerUserId},</if>
            <if test="manufacturerName != null and manufacturerName != ''">#{manufacturerName},</if>
            <if test="manufacturerAvatar != null">#{manufacturerAvatar},</if>
            <if test="proofingInvitationId != null">#{proofingInvitationId},</if>
            <if test="basePrice != null">#{basePrice},</if>
            <if test="tieredPricing != null">#{tieredPricing},</if>
            <if test="validityDays != null">#{validityDays},</if>
            <if test="status != null">#{status},</if>
            <if test="productionStatus != null">#{productionStatus},</if>
            <if test="deliveryStatus != null">#{deliveryStatus},</if>
            <if test="viewCount != null">#{viewCount},</if>
            <if test="favoriteCount != null">#{favoriteCount},</if>
            <if test="shareCount != null">#{shareCount},</if>
            <if test="manufacturerPhotos != null">#{manufacturerPhotos},</if>
            <if test="manufacturerUploadTime != null">#{manufacturerUploadTime},</if>
            <if test="totalSalesAmount != null">#{totalSalesAmount},</if>
            <if test="createBy != null">#{createBy},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="updateBy != null">#{updateBy},</if>
            <if test="updateTime != null">#{updateTime},</if>
        </trim>
    </insert>

</mapper>
