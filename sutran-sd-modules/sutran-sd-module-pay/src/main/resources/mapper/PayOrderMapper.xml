<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sutran.sd.pay.mapper.PayOrderMapper">

    <update id="failPay">
        UPDATE pay_order
        SET trade_no = #{tradeNo},<if test='totalAmount != null'>total_amount = #{totalAmount},</if> status = 2
        WHERE out_trade_no = #{outTradeNo}
    </update>

</mapper>
