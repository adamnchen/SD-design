<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sutran.sd.system.mapper.SysUserRoleMapper">

    <resultMap type="SysUserRole" id="SysUserRoleResult">
        <result property="userId" column="user_id"/>
        <result property="roleId" column="role_id"/>
    </resultMap>
    <select id="selectUserIdsByRoleId" resultType="Long">
        select u.user_id from sys_user u
        inner join sys_user_role sur
            on u.user_id = sur.user_id and sur.role_id = #{roleId}
    </select>

    <select id="selectAdminUserOpenId" resultType="java.util.Map">
        SELECT DISTINCT CONVERT(B.user_id,CHAR) AS userId,B.wx_open_id AS wxOpenId FROM sys_user_role AS A LEFT JOIN sys_user AS B ON A.user_id=B.user_id WHERE A.role_id IN (1,3);
    </select>

</mapper>
